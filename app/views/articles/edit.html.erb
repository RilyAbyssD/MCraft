<% content_for :title do %>
  MCraft｜<%= @article.title %>の編集
<% end %>
<div class="container">
  <h1 class="text-center margin-top-bottom-form">募集内容を変更する</h1>
  <%= form_for @article do |f| %>

    <div class="form-group padding-top-15">
      <% if @article.image? %>
        <%= image_tag @article.image.url, class: "img-fluid" %>
      <% else %>
        <%= image_tag "default.png"%>
      <% end %>
      <%= f.file_field :image %>
      <p>※選択しても表示されている画像は変わりませんが投稿すると、表示されます。</p>
    </div>
    <div class="form-group padding-top-15">
      <%= f.label(:title, "サーバー名を記入してください。「〇〇サーバー運営募集！！」等の文章は、ご控えください)", class: "form-label margin-top-30px", style: "font-size: 18px") %>
      <%= f.text_field :title, class: "form-control" %>
    </div>
    <div class="form-group padding-top-15">
      <%= f.label :body, "内容(今後、マークダウンでの作成が可能になる予定です)", class: "form-label margin-top-30px", style: "font-size: 18px" %>
      <%= f.text_area :body, rows: 10, class: "form-control" %>
    </div>
    <%= f.submit "編集して更新する", class: "btn btn-success", style: "margin-top: 35px; margin-bottom: 35px;" %>
<% end %>
</div>